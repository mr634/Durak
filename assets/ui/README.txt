Place in this folder (Durak/assets/ui/):

  table_background.png
  button_background.png

Served as /assets/ui/<filename>

If you replace a PNG and the browser still shows the old image: restart the server after changing server.js cache rules, then hard-refresh (Ctrl+Shift+R). Production: set env ASSET_CACHE=1 for long-lived caching.
